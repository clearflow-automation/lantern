# Lantern — studio site

The public Lantern marketing site. Static, no build step.
Deployed to Cloudflare Workers static assets (`public/` → `lantern.lanternstudio.workers.dev`).

Prospect demos live separately in `clearflow-automation/lantern-site`.
